{
  "name": "package-sort",
  "version": "1.0.0",
  "description": "Robotic arm package dispatch by volume and mass",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
