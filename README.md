[**Open in StackBlitz**](https://stackblitz.com/~/github.com/zamor122/thoughtful-ai-prompt?file=src/sort.ts)

1. Click the link above to open the project in your browser.
2. In the terminal panel, run:
   ```bash
   npm install
   npm run test
   ```
3. All 21 tests should pass. To run the `sort` function directly, open `src/sort.ts` and use the browser console or add a quick test in the file.